apiVersion: 1
datasources:
  # This is the datasource configuration for Grafana, alternatively you can use the UI to add datasource.
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true